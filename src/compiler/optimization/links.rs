use petgraph::stable_graph::{EdgeIndex, NodeIndex};

use crate::types::compiler::{Graph, Sources};

pub fn remove_unused_links(graph: Graph, power_sources: Sources) -> Graph {
    let removed_edges: Vec<EdgeIndex> = vec![];
    todo!()
}
